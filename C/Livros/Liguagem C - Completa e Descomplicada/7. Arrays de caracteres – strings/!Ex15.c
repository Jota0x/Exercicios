/*
Escreva um programa que leia duas strings e as imprima em ordem alfabética, a
ordem em que elas apareceriam em um dicionário.
*/
#include <stdio.h>
#include <stdlib.h>

int main()
{
    char *palavra1 = (char *)malloc(100 * sizeof(char));
    char *palavra2 = (char *)malloc(100 * sizeof(char));

    scanf("%s", palavra1);
    scanf("%s", palavra2);

    for(int i = 0; i )


    return 0;
} // end main